"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.aspectToPadding = (aspect) => {
    const _split = aspect.split(":");
    if (_split.length !== 2)
        return "0%";
    const _left = parseInt(_split[0].trim());
    const _right = parseInt(_split[1].trim());
    if (isNaN(_left) === true || isNaN(_right) === true)
        return "0%";
    return (_right / _left) * 100 + "%";
};
