import { Chain, SignTransactionConfig, SignTransactionResponse, User } from 'universal-authenticator-library';
export declare class LynxUser extends User {
    private account;
    private keys;
    private chainId;
    constructor(chain: Chain | null, accountObj: any);
    signTransaction(transaction: any, _config: SignTransactionConfig): Promise<SignTransactionResponse>;
    signArbitrary(_: string, data: string, helpText: string): Promise<string>;
    verifyKeyOwnership(_: string): Promise<boolean>;
    getAccountName(): Promise<string>;
    getChainId(): Promise<string>;
    getKeys(): Promise<string[]>;
}
